import { useWallet } from '../../context/WalletContext';
export default function Overview() { const { wallet } = useWallet(); const data=[['Available balance',wallet.balance],['Activity rewards',wallet.profits],['Held',wallet.frozen]]; return <section className="mx-4 rounded-3xl bg-navy p-5 text-white"><p className="text-sm text-white/60">Demo portfolio</p><h1 className="mt-1 text-3xl font-bold">${wallet.balance.toLocaleString()}</h1><div className="mt-5 grid grid-cols-3 gap-2 border-t border-white/10 pt-4">{data.map(([k,v])=><div key={k}><p className="text-[10px] text-white/55">{k}</p><b className="text-sm">${v}</b></div>)}</div></section>; }

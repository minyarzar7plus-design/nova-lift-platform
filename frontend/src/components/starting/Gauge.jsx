import { useWallet } from '../../context/WalletContext';
export default function Gauge() { const { wallet } = useWallet(); const pct=Math.round(wallet.completed/wallet.total*100); return <section className="card mx-4 flex items-center gap-4"><div className="grid h-16 w-16 place-items-center rounded-full" style={{background:`conic-gradient(#45d6ad ${pct*3.6}deg,#eef2f7 0)`}}><div className="grid h-12 w-12 place-items-center rounded-full bg-white text-sm font-bold">{pct}%</div></div><div><h2 className="font-bold">Today’s progress</h2><p className="mt-1 text-sm text-slate-500">{wallet.completed} of {wallet.total} sample tasks complete</p></div></section>; }
